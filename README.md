# whynotusethis.website

Персональный сайт-портфолио с функционалом временного контента (self-destructing content).

## 🏗 Структура проекта

```
whynotusethiswebsite/
├── index.html              # Главная страница (только HTML)
├── view.html               # Страница просмотра временного контента
├── css/
│   └── styles.css          # Все стили
├── js/
│   ├── app.js              # 3D фон, GSAP, glitch-эффекты, генератор идей
│   └── content-modal.js    # Логика модального окна временного контента
├── backend/                # Node.js API сервер
│   ├── server.js           # Express сервер с API endpoints
│   ├── database.js         # SQLite база данных
│   ├── package.json        # Зависимости
│   ├── data/               # SQLite файлы (создаётся автоматически)
│   └── uploads/            # Загруженные файлы (создаётся автоматически)
└── deploy/                 # Конфигурация деплоя
    ├── nginx.conf          # Nginx конфиг (за Xray на порту 8080)
    ├── whynotusethis-backend.service  # Systemd сервис
    └── deploy.sh           # Скрипт развёртывания
```

## 🚀 Основные функции

### 1. Временный контент (`/view/:uuid`)
- **Текст или ссылка** — вводится в одно поле, ссылки определяются автоматически
- **Файлы** — изображения (PNG, JPG, GIF, WEBP до 10MB)

### 2. Режимы удаления
| Режим | Описание |
|-------|----------|
| **Автоудаление по таймеру** | Удаление через 1 мин / 5 мин / 15 мин / 1 час / 24 часа после первого просмотра |
| **Burn After Read** 🔥 | Мгновенное удаление после первого просмотра |
| **IP Restriction** 🔒 | Один просмотр с одного IP-адреса |

## 🔧 API Endpoints

### `POST /api/content`
Создание нового контента.

**Body (FormData):**
```json
{
  "type": "text|link|file",  // auto-detected: link if starts with http/www
  "content": "текст или URL",
  "file": "<binary>",
  "autoDelete": "true|false",
  "deleteAfterMinutes": "1|5|15|60|1440",
  "burnAfterRead": "true|false",
  "ipRestriction": "true|false"
}
```

**Response:**
```json
{
  "success": true,
  "uuid": "abc-123-def",
  "url": "/view/abc-123-def"
}
```

### `GET /api/content/:uuid`
Получение контента. При первом просмотре:
- Запускает таймер (если `autoDelete`)
- Записывает IP (если `ipRestriction`)
- Удаляет контент после ответа (если `burnAfterRead`)

### `GET /api/content/:uuid/download`
Скачивание файла.

### `DELETE /api/content/:uuid`
Ручное удаление контента.

## 💾 База данных (SQLite)

### Таблица `contents`
| Поле | Тип | Описание |
|------|-----|----------|
| id | INTEGER | PK |
| uuid | TEXT | Уникальный идентификатор |
| type | TEXT | text, link, file |
| content | TEXT | Текст/URL |
| file_path | TEXT | Путь к файлу |
| file_name | TEXT | Оригинальное имя файла |
| auto_delete | INTEGER | 0/1 |
| delete_after_minutes | INTEGER | Время в минутах |
| burn_after_read | INTEGER | 0/1 |
| ip_restriction | INTEGER | 0/1 |
| first_viewed_at | INTEGER | Unix timestamp первого просмотра |
| created_at | INTEGER | Unix timestamp создания |
| deleted | INTEGER | 0/1 soft delete |

### Таблица `content_views`
| Поле | Тип | Описание |
|------|-----|----------|
| id | INTEGER | PK |
| content_id | INTEGER | FK → contents.id |
| ip_address | TEXT | IP-адрес посетителя |
| viewed_at | INTEGER | Unix timestamp |

## 🖥 Локальная разработка

```bash
# Установить зависимости backend
cd backend && npm install

# Запустить сервер (порт 3001)
npm start

# Открыть index.html в браузере
# API будет на http://localhost:3001
```

## 📦 Deploy на VPS

```bash
ssh root@23.95.75.54
cd /var/www/whynotusethis.website
git pull origin main
chmod +x deploy/deploy.sh
sudo ./deploy/deploy.sh
```

**Проверка:**
```bash
systemctl status whynotusethis-backend
curl http://localhost:3001/api/health
```

## 🎨 Frontend стек

- **Tailwind CSS** — стилизация
- **Three.js** — 3D фон с звёздами и глитч-эффектами
- **GSAP** — анимации
- **Vanilla JS** — логика модального окна и форм

## 📝 TODO для будущей разработки

- [ ] Защита паролем для контента
- [ ] Rate limiting для API
- [ ] HTTPS через Let's Encrypt (сейчас через Xray)

## 🔐 Безопасность

- Файлы хранятся вне web root
- IP получается из `X-Forwarded-For` / `X-Real-IP` заголовков
- Загрузка только изображений (проверка MIME type)
- Soft delete для возможности аудита
- Периодическая очистка expired контента каждые 30 сек
